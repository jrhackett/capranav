package testing;
/**
 * Created by Henry on 11/15/2015.
 */
public class GraphTesting {
   /* Graph testGraph1;
    Graph testGraph2;
    Graph freshGraph;
    HashMap<Integer, Node> hash1;
    LinkedList<Node> nodes2 = new LinkedList<Node>();
    Node node1, node2, node3, node4;

    @Before
    public void setUp() throws Exception {
        node1 = new Node("X", 1, 0, 0, 0);
        node2 = new Node("Y", 2, 0, 0, 0);
        node3 = new Node("Z", 5, 5, 5, 7);
        node4 = new Node("A", 2, 3, 4, 5);

        hash1 = new HashMap<Integer, Node>();
        hash1.put(1, node1);
        hash1.put(2, node2);
        hash1.put(3, node3);
        hash1.put(4, node4);

        nodes2.add(node1);
        nodes2.add(node2);
        nodes2.add(node3);
        nodes2.add(node4);

        freshGraph = new Graph();
        testGraph1 = new Graph(hash1);
        testGraph2 = new Graph();
    }

    @Test
    public void toStringTest1(){
        assertEquals("toString on an empty graph returned incorrect value", "", freshGraph.toString());
    }

    @Test
    public void toStringTest2(){
        assertEquals("toString on a non empty graph returned incorrect value", "X Y Z A ", testGraph1.toString());
    }

    @Test
    public void getNodesTest1(){
        assertEquals("getNodes on an empty graph returned incorrect value", new HashMap<Integer, Node>(), freshGraph.getNodes());
    }

    @Test
    public void getNodesTest2(){
        assertEquals("getNodes on a non empty graph returned incorrect value", hash1, testGraph1.getNodes());
    }

    *//*
    @Test
    public void getTest1(){
        assertEquals("get on an empty graph returned incorrect value", new HashMap<Node>(), freshGraph.get());
    }

    @Test
    public void getTest2(){
        assertEquals("get on a non empty graph returned incorrect value", nodes2, testGraph1.get());
    }
    */
}
